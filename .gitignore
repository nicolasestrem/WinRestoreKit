# NuGet restore output (packages.config)
src/packages/

# Build outputs. These were historically tracked in git; commit 488060e untracked them.
[Bb]in/
[Oo]bj/

# Release artifact directory used by the /release skill (a ~69 MB self-contained exe).
publish/

# dotnet test output: .trx logs, and the crash/hang dumps --blame-hang writes, which run to
# hundreds of megabytes each.
[Tt]est[Rr]esults/

# Visual Studio user/workspace files
.vs/
*.user
*.suo

# Per-user Claude Code settings (machine-specific paths, local permission grants).
# The rest of .claude/ - hooks, skills, agents - is shared project tooling and is tracked.
.claude/settings.local.json
WATCHDOG.yml
/.logs
/.superpowers
